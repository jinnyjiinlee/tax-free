# 텍스프리 (Tax Free) 프로젝트 규칙

## 브랜치 전략
- **main**: 개발 작업 브랜치 (기본 작업 브랜치)

## 작업 흐름
1. `main` 브랜치에서 작업
2. 작업 전 `git pull origin main`으로 최신 코드 받기
3. 변경사항 커밋: `git add . && git commit -m "메시지"`
4. 푸시: `git push origin main`

## 커밋 메시지 형식
- `feat: 새로운 기능 추가`
- `fix: 버그 수정`
- `style: 코드 스타일 변경`
- `refactor: 리팩토링`
- `docs: 문서 수정`

## 프로젝트 구조
- `app/`: Next.js App Router 구조
- `app/components/`: React 컴포넌트
- `app/api/`: API 라우트
- `app/data/`: 데이터 파일

## 주의사항
- `.env*.local` 파일은 커밋하지 않기 (이미 .gitignore에 포함됨)
- `node_modules/`는 커밋하지 않기
- 빌드 전에 `npm run build`로 확인하기
